---
title: "ST Analysis: Doctorates"
output: html_document
---
  
```{r setup, message=FALSE, warning=FALSE, include=FALSE}

# List of required packages
required_packages <- c(
  "tidyverse", "readxl", "openxlsx", "survival", "cmprsk", 
  "writexl", "MASS", "bife", "plm", "margins", "broom"
)

# Function to check and install missing packages
install_if_missing <- function(pkg) {
  if (!require(pkg, character.only = TRUE)) {
    install.packages(pkg, dependencies = TRUE)
    library(pkg, character.only = TRUE)
  }
}

# Apply the function to all required packages
invisible(lapply(required_packages, install_if_missing))


# Function to extract academic year and term
extract_year_and_term <- function(data) {
  data <- data %>%
    mutate(Year = substr(ADMIT_TERM, 2, 5),
           Term = substr(ADMIT_TERM, 1, 1))
  
  # Convert to factors for use in the model
  data$Year <- as.factor(data$Year)
  data$Term <- as.factor(data$Term)
  
  return(data)
}




# Fit Linear Model (LM)
fit_lm_model <- function(data, outcome_variable, filter_stem = NULL) {
  # Extract academic year and term
  data <- extract_year_and_term(data)
  
  # Drop rows with missing outcome variable
  data <- data %>% drop_na(!!sym(outcome_variable))
  
  # Filter data if filter_stem is provided
  if (!is.null(filter_stem)) {
    data <- data %>% filter(STEM == filter_stem)
  }
  
  # Convert to factors if not already
  data$Term <- as.factor(data$Term)
  data$Year <- as.factor(data$Year)
  data$Dept <- as.factor(data$Dept)
  
  # Create dummy variables
  term_dummies <- model.matrix(~ Term - 1, data = data)
  year_dummies <- model.matrix(~ Year - 1, data = data)
  dept_dummies <- model.matrix(~ Dept - 1, data = data)
  
  # Bind the dummy variables with the original data
  data <- cbind(data, term_dummies, year_dummies, dept_dummies) %>%
    dplyr::select(-c(Term, Year, Dept))
  
  # Verify the column names of the dummy variables
  term_columns <- colnames(data)[grep("^Term", colnames(data))]
  year_columns <- colnames(data)[grep("^Year", colnames(data))]
  dept_columns <- colnames(data)[grep("^Dept", colnames(data))]
  dept_columns <- dept_columns[(dept_columns != "Dept_code") &
                               (dept_columns != "Dept")]
  dept_columns <- sapply(dept_columns, function(x) if (grepl("[^A-Za-z0-9_]", x)) paste0("`", x, "`") else x)

  # Construct the formula with all dummy variables explicitly
  formula <- as.formula(paste(outcome_variable, "~ st_required + prev_deg +", 
                              paste(c(term_columns, year_columns, dept_columns), collapse = " + ")))
  
  # Fit the linear model
  model <- lm(formula, data = data)

  # Get the summary of the model
  summary_model <- tidy(model)
  
  colnames(summary_model) <- c("Term", "Estimate", "SE", "z", "p")
  
  # Add sample size to the summary
  sample_size <- nobs(model)
  new_row <- tibble(
    Term = "Sample Size",
    Estimate = sample_size
  ) %>%
    add_column(!!!setNames(rep(NA, ncol(summary_model) - 2), 
                           colnames(summary_model)[-(1:2)]))
  summary_model <- bind_rows(new_row, summary_model)
  
  summary_model$Term <- gsub("`", "", summary_model$Term)
  
  # Extract estimates for 'st_required'
  coef_estimate <- coef(model)['st_required']
  p_value <- coef(summary(model))['st_required', 'Pr(>|t|)']
  entry <- paste0(coef_estimate, ' (', p_value, ')')
  
  list(entry = entry, summary_model = summary_model)
}



# Fit Generalized Linear Model (GLM)
fit_glm_model <- function(data, outcome_variable, filter_stem = NULL) {
  # Extract academic year and term
  data <- extract_year_and_term(data)
  
  # Drop rows with missing outcome variable
  data <- data %>% drop_na(!!sym(outcome_variable))
  
  # Filter data if filter_stem is provided
  if (!is.null(filter_stem)) {
    data <- data %>% filter(STEM == filter_stem)
  }
  
  # Convert to factors
  data$Term <- as.factor(data$Term)
  data$Year <- as.factor(data$Year)
  data$Dept <- as.factor(data$Dept)
  
  # Manually set reference level to avoid multicolinearity
  data$Term <- relevel(data$Term, ref = "X")
  data$Year <- relevel(data$Year, ref = "2003")
  
  # Create dummy variables
  # term_dummies <- model.matrix(~ Term - 1, data = data)
  # year_dummies <- model.matrix(~ Year - 1, data = data)
  dept_dummies <- model.matrix(~ Dept - 1, data = data)

  # Bind the dummy variables with the original data
  data <- cbind(data, dept_dummies)

  # Verify the column names of the dummy variables
  # term_columns <- colnames(data)[grep("^Term", colnames(data))]
  # year_columns <- colnames(data)[grep("^Year", colnames(data))]
  dept_columns <- colnames(data)[grep("^Dept", colnames(data))]
  dept_columns <- dept_columns[(dept_columns != "Dept_code") &
                               (dept_columns != "Dept")]
  dept_columns <- sapply(dept_columns, function(x) {
  if (grepl("[^A-Za-z0-9_]", x)) {
    paste0("`", x, "`")  # Add backticks around columns with special characters
  } else {
    x
  }
})

  # Construct the formula with all dummy variables explicitly
  formula <- as.formula(paste(outcome_variable, "~ st_required + prev_deg + Term + Year +",
                              paste(c(dept_columns), collapse = " + ")))
  
  # Fit the model using the data with manually created dummy variables
  model <- glm(formula, data = data, family = binomial(link = "logit"))
  
  # x <- model.matrix(formula, data = data)
  # y <- data[[outcome_variable]]
  # lasso_model <- glmnet(x, y, family = "binomial")
  # set.seed(123)  
  # cv_model <- cv.glmnet(x, y, family = "binomial")
  # best_lambda <- cv_model$lambda.min
  # lasso_model_best <- glmnet(x, y, family = "binomial", lambda = best_lambda)

  # Extract coefficients and their standard errors
  summary_model <- tidy(model)
  
  summary_model$term <- gsub("`", "", summary_model$term)
  
  # Calculate predicted probabilities
  data$predicted_probs <- predict(model, type = "response")
  
  # Compute the marginal effects for binary factors
  # Marginal effect = β * p * (1 - p)
  marginal_effects <- summary_model %>%
    mutate(
      # For binary factors, the marginal effect is β * p * (1 - p)
      Estimate = ifelse(term %in% names(coef(model)), 
                         estimate * mean(data$predicted_probs) * (1 - mean(data$predicted_probs)), 
                         NA),
      SE = std.error,
      z = statistic,
      p = p.value
    )
  
  # Rename columns as needed
  marginal_summary <- marginal_effects %>%
    dplyr::select(
      Term = term,
      Estimate,
      SE,
      z,
      p
    )
  
  sample_size <- nobs(model)
  # Create a new row for sample size
  new_row <- tibble(
    Term = "Sample Size",
    Estimate = sample_size
  ) %>%
    add_column(!!!setNames(rep(NA, ncol(marginal_summary) - 2), 
                           colnames(marginal_summary)[-(1:2)]))
  
  # Bind the new row to the existing data frame
  marginal_summary <- bind_rows(new_row, marginal_summary)
  
  # Extract marginal effects for 'st_required'
  marginal <- filter(marginal_summary, Term == 'st_required')$Estimate
  p_value <- filter(marginal_summary, Term == 'st_required')$p
  marginal_entry <- paste0(marginal, ' (', p_value, ')')
  
  return(list(
    marginal_summary = marginal_summary,
    marginal_entry = marginal_entry
  ))
}

```


# Import data

```{r message=FALSE, warning=FALSE, include=FALSE}

doc_dat5 <- read.xlsx("doc_dat5.xlsx")
doc_dat6 <- read.xlsx("doc_dat6.xlsx")

clean_dept_column <- function(df) {
  df$Dept <- gsub('xml:space=\\"preserve\\">', '', df$Dept)
  df$Dept <- gsub('&amp; ', '', df$Dept)
  df$Dept <- trimws(df$Dept)  # Remove leading/trailing whitespace
  return(df)
}

doc_dat5 <- clean_dept_column(doc_dat5)
doc_dat6 <- clean_dept_column(doc_dat6)

```


# Descriptive statistics

```{r message=FALSE, warning=FALSE, include=FALSE}

# Function to generate histograms and save them as PNG files with optional filtering by st_required
generate_histogram <- function(data, column, title, xlabel, filename, filter_stem = NULL, st_required_filter = NULL) {
  if (!is.null(st_required_filter)) {
    data <- data %>% filter(st_required == st_required_filter)
  }
  
  if (!is.null(filter_stem)) {
    data <- data %>% filter(STEM == filter_stem)
  }
  
  png(filename = filename)
  hist(data[[column]], main = title, xlab = xlabel, ylab = "Frequency")
  dev.off()
}

# Function to calculate summary statistics for a given list of variables with optional filtering
calculate_summary <- function(data5, data6, filter_stem = NULL, st_required_filter = NULL) {
  
  if (!is.null(filter_stem)) {
    data5 <- data5 %>% filter(STEM == filter_stem)
    data6 <- data6 %>% filter(STEM == filter_stem)
  }
  
  if (!is.null(st_required_filter)) {
    data5 <- data5 %>% filter(st_required == st_required_filter)
    data6 <- data6 %>% filter(st_required == st_required_filter)
  }

  # Base variables that should only appear once
  variable_list <- list(
    st_required = data6$st_required,
    year_1_gpa = data5$year_1_gpa,
    year_2_gpa = data5$year_2_gpa,
    URM = data6$URM,
    Non_Male = data6$Non_Male,
    TIME_DEG = data6$TIME_DEG,
    completed_8 = data6$completed_8,
    completed_10 = data6$completed_10
  )
  
  # Iterate over the years and add year-specific 'separated_i' values
  for (year_i in 1:10) {
    # Filter the data based on year and ADMIT_TERM conditions
    data6_i <- data6 %>%
      filter(case_when(
        substr(ADMIT_TERM, 1, 1) %in% c("F", "X") ~ as.numeric(substr(ADMIT_TERM, nchar(ADMIT_TERM) - 3, nchar(ADMIT_TERM))) < 2023 - year_i,
        substr(ADMIT_TERM, 1, 1) %in% c("W", "S") ~ as.numeric(substr(ADMIT_TERM, nchar(ADMIT_TERM) - 3, nchar(ADMIT_TERM))) < 2024 - year_i,
        TRUE ~ FALSE
      ))
    
    # Add the year-specific 'separated_i' variable to the result list
    variable_list[[paste0("separated_", year_i)]] <- data6_i[[paste0("separated_", year_i)]]
  }

  
  stats <- lapply(variable_list, function(variable) {
    list(
      `Sample Size` = sub("\\.?0+$", "", as.character(sum(!is.na(variable)))),  # Calculate and add the sample size
      Mean = (mean(variable, na.rm = TRUE)),
      Median = (median(variable, na.rm = TRUE)),
      SD = (sd(variable, na.rm = TRUE)),
      Min = min(variable, na.rm = TRUE),
      Max = max(variable, na.rm = TRUE),
      Quantiles = paste(quantile(variable, probs = c(0.25, 0.5, 0.75), na.rm = TRUE), collapse = ", ")
    )
  })

  # Convert the list of statistics to a data frame
  summary_model <- do.call(rbind, lapply(stats, as.data.frame))
  summary_model$Variable <- c('ST Required', 'Year 1 GPA', 'Year 2 GPA', 'URM', 'Non-Male', 'Time to Degree', 'Completed within 8 years', 'Completed within 10 years', 'Year 1 Separation', 'Year 2 Separation', 'Year 3 Separation', 'Year 4 Separation', 'Year 5 Separation', 'Year 6 Separation', 'Year 7 Separation', 'Year 8 Separation', 'Year 9 Separation', 'Year 10 Separation')
  # Reorder columns and reset rownames
  summary_model <- summary_model[, c("Variable", setdiff(names(summary_model), "Variable"))]
  rownames(summary_model) <- NULL
  return(summary_model)
}

# Reusable function to generate histograms and summary statistics with stratification
generate_analysis <- function(data5, data6, prefix, filter_stem = NULL) {
  
  # Histograms with stratification
  generate_histogram(data5, "year_1_gpa", "Histogram of First-Year GPA", "First-Year GPA", 
                     paste0(dirname(getwd()), "/Shiny/www/", prefix, "_gpa1_hist.png"), filter_stem)
  generate_histogram(data5, "year_1_gpa", "Histogram of First-Year GPA (ST Required = 1)", "First-Year GPA", 
                     paste0(dirname(getwd()), "/Shiny/www/", prefix, "_gpa1_hist_1.png"), filter_stem, st_required_filter = 1)
  generate_histogram(data5, "year_1_gpa", "Histogram of First-Year GPA (ST Required = 0)", "First-Year GPA", 
                     paste0(dirname(getwd()), "/Shiny/www/", prefix, "_gpa1_hist_0.png"), filter_stem, st_required_filter = 0)

  generate_histogram(data5, "year_2_gpa", "Histogram of Second-Year GPA", "Second-Year GPA", 
                     paste0(dirname(getwd()), "/Shiny/www/", prefix, "_gpa2_hist.png"), filter_stem)
  generate_histogram(data5, "year_2_gpa", "Histogram of Second-Year GPA (ST Required = 1)", "Second-Year GPA", 
                     paste0(dirname(getwd()), "/Shiny/www/", prefix, "_gpa2_hist_1.png"), filter_stem, st_required_filter = 1)
  generate_histogram(data5, "year_2_gpa", "Histogram of Second-Year GPA (ST Required = 0)", "Second-Year GPA", 
                     paste0(dirname(getwd()), "/Shiny/www/", prefix, "_gpa2_hist_0.png"), filter_stem, st_required_filter = 0)

  generate_histogram(data6, "TIME_DEG", "Histogram of Time to Degree", "Time to Degree", 
                     paste0(dirname(getwd()), "/Shiny/www/", prefix, "_time_hist.png"), filter_stem)
  generate_histogram(data6, "TIME_DEG", "Histogram of Time to Degree (ST Required = 1)", "Time to Degree", 
                     paste0(dirname(getwd()), "/Shiny/www/", prefix, "_time_hist_1.png"), filter_stem, st_required_filter = 1)
  generate_histogram(data6, "TIME_DEG", "Histogram of Time to Degree (ST Required = 0)", "Time to Degree", 
                     paste0(dirname(getwd()), "/Shiny/www/", prefix, "_time_hist_0.png"), filter_stem, st_required_filter = 0)

  # Summary Stats with and without stratification
  summary <- calculate_summary(data5, data6, filter_stem)
  summary_1 <- calculate_summary(data5, data6, filter_stem, st_required_filter = 1)
  summary_0 <- calculate_summary(data5, data6, filter_stem, st_required_filter = 0)
  
  list(summary = summary, summary_1 = summary_1, summary_0 = summary_0)
}

# Generate analysis for Doctorate, STEM, and Non-STEM with stratification
doc_analysis <- generate_analysis(doc_dat5, doc_dat6, "doc")
doc_stem_analysis <- generate_analysis(doc_dat5, doc_dat6, "doc_stem", filter_stem = 1)
doc_non_stem_analysis <- generate_analysis(doc_dat5, doc_dat6, "doc_non_stem", filter_stem = 0)

```




# GPA

```{r message=FALSE, warning=FALSE, include=FALSE}

# Year 1 GPA Analysis

doc_gpa1 <- fit_lm_model(doc_dat5, "year_1_gpa")
doc_gpa1_entry <- doc_gpa1$entry
doc_gpa1_model_summary <- doc_gpa1$summary_model

doc_stem_gpa1 <- fit_lm_model(doc_dat5, "year_1_gpa", filter_stem = 1)
doc_stem_gpa1_entry <- doc_stem_gpa1$entry
doc_stem_gpa1_model_summary <- doc_stem_gpa1$summary_model

doc_non_stem_gpa1 <- fit_lm_model(doc_dat5, "year_1_gpa", filter_stem = 0)
doc_non_stem_gpa1_entry <- doc_non_stem_gpa1$entry
doc_non_stem_gpa1_model_summary <- doc_non_stem_gpa1$summary_model

# Year 2 GPA Analysis

doc_gpa2 <- fit_lm_model(doc_dat5, "year_2_gpa")
doc_gpa2_entry <- doc_gpa2$entry
doc_gpa2_model_summary <- doc_gpa2$summary_model

doc_stem_gpa2 <- fit_lm_model(doc_dat5, "year_2_gpa", filter_stem = 1)
doc_stem_gpa2_entry <- doc_stem_gpa2$entry
doc_stem_gpa2_model_summary <- doc_stem_gpa2$summary_model

doc_non_stem_gpa2 <- fit_lm_model(doc_dat5, "year_2_gpa", filter_stem = 0)
doc_non_stem_gpa2_entry <- doc_non_stem_gpa2$entry
doc_non_stem_gpa2_model_summary <- doc_non_stem_gpa2$summary_model

```



# URM

```{r message=FALSE, warning=FALSE, include=FALSE}

# Doctorate Overall
doc_URM <- fit_glm_model(doc_dat6, "URM")
doc_URM_entry <- doc_URM$marginal_entry
doc_URM_summary <- doc_URM$marginal_summary

# Doctorate STEM
doc_stem_URM <- fit_glm_model(doc_dat6, "URM", filter_stem = 1)
doc_stem_URM_entry <- doc_stem_URM$marginal_entry
doc_stem_URM_summary <- doc_stem_URM$marginal_summary

# Doctorate Non-STEM
doc_non_stem_URM <- fit_glm_model(doc_dat6, "URM", filter_stem = 0)
doc_non_stem_URM_entry <- doc_non_stem_URM$marginal_entry
doc_non_stem_URM_summary <- doc_non_stem_URM$marginal_summary

```


# Non-Male

```{r message=FALSE, warning=FALSE, include=FALSE}

# Year Non_Male Analysis

# Doctorate Overall
doc_Non_Male <- fit_glm_model(doc_dat6, "Non_Male")
doc_Non_Male_summary <- doc_Non_Male$marginal_summary
doc_Non_Male_entry <- doc_Non_Male$marginal_entry

# Doctorate STEM
doc_stem_Non_Male <- fit_glm_model(doc_dat6, "Non_Male", filter_stem = 1)
doc_stem_Non_Male_summary <- doc_stem_Non_Male$marginal_summary
doc_stem_Non_Male_entry <- doc_stem_Non_Male$marginal_entry

# Doctorate Non-STEM
doc_non_stem_Non_Male <- fit_glm_model(doc_dat6, "Non_Male", filter_stem = 0)
doc_non_stem_Non_Male_summary <- doc_non_stem_Non_Male$marginal_summary
doc_non_stem_Non_Male_entry <- doc_non_stem_Non_Male$marginal_entry

```


# TIME_DEG

## Linear regression

```{r message=FALSE, warning=FALSE, include=FALSE}

doc_time <- fit_lm_model(doc_dat6, "TIME_DEG")
doc_time_entry <- doc_time$entry
doc_time_model_summary <- doc_time$summary_model

doc_time_stem <- fit_lm_model(doc_dat6, "TIME_DEG", filter_stem = 1)
doc_stem_time_entry <- doc_time_stem$entry
doc_stem_time_model_summary <- doc_time_stem$summary_model

doc_time_non_stem <- fit_lm_model(doc_dat6, "TIME_DEG", filter_stem = 0)
doc_non_stem_time_entry <- doc_time_non_stem$entry
doc_non_stem_time_model_summary <- doc_time_non_stem$summary_model

```


## Survival

```{r message=FALSE, warning=FALSE, include=FALSE}
# 
# # Doctorate
# 
# doc_dat6 <- doc_dat6[!is.na(doc_dat6$event), ] %>%
#   mutate(event = ifelse(event == 1 | event == 4, 0, event)) %>%
#   mutate(event = ifelse(event == 2, 1, event)) %>%
#   mutate(event = ifelse(event == 3, 2, event)) %>%
#   mutate(TIME_DEG = ifelse(is.na(TIME_DEG), max(doc_dat6[!is.na(doc_dat6$TIME_DEG), ]$TIME_DEG), TIME_DEG))
# 
# doc_dat6$Dept <- as.numeric(as.factor(doc_dat6$Dept))
# doc_dat6$ADMIT_TERM <- as.numeric(as.factor(doc_dat6$ADMIT_TERM))
# 
# doc_crr_fit <- crr(ftime = doc_dat6$TIME_DEG, 
#     fstatus = doc_dat6$event, 
#     cov1 = data.frame(st_required = doc_dat6$st_required,
#                       Dept = doc_dat6$Dept,
#                       ADMIT_TERM = doc_dat6$ADMIT_TERM))
# 
# doc_crr_fit_summary <- summary(doc_crr_fit)
# doc_crr_fit_summary <- summary_to_df_surv(doc_crr_fit_summary)
# doc_crr_fit_hr <- round_sig(doc_crr_fit_summary$coef[1,2])
# doc_crr_fit_p <- round_sig(doc_crr_fit_summary$coef[1,5])
# doc_crr_fit_entry <- paste0(doc_crr_fit_hr, ' (', doc_crr_fit_p, ')')
# 
# 
# # Doctorate STEM
# 
# doc_dat6_stem <- doc_dat6 %>%
#   filter(STEM == 1)
# 
# doc_dat6_stem <- doc_dat6_stem[!is.na(doc_dat6_stem$event), ] %>%
#   mutate(event = ifelse(event == 1 | event == 4, 0, event)) %>%
#   mutate(event = ifelse(event == 2, 1, event)) %>%
#   mutate(event = ifelse(event == 3, 2, event)) %>%
#   mutate(TIME_DEG = ifelse(is.na(TIME_DEG), max(doc_dat6_stem[!is.na(doc_dat6_stem$TIME_DEG), ]$TIME_DEG), TIME_DEG))
# 
# doc_dat6_stem$Dept <- as.numeric(as.factor(doc_dat6_stem$Dept))
# doc_dat6_stem$ADMIT_TERM <- as.numeric(as.factor(doc_dat6_stem$ADMIT_TERM))
# 
# doc_stem_crr_fit <- crr(ftime = doc_dat6_stem$TIME_DEG, 
#     fstatus = doc_dat6_stem$event, 
#     cov1 = data.frame(st_required = doc_dat6_stem$st_required,
#                       Dept = doc_dat6_stem$Dept,
#                       ADMIT_TERM = doc_dat6_stem$ADMIT_TERM))
# 
# doc_stem_crr_fit_summary <- summary(doc_stem_crr_fit)
# doc_stem_crr_fit_summary <- summary_to_df_surv(doc_stem_crr_fit_summary)
# doc_stem_crr_fit_hr <- round_sig(doc_stem_crr_fit_summary$coef[1,2])
# doc_stem_crr_fit_p <- round_sig(doc_stem_crr_fit_summary$coef[1,5])
# doc_stem_crr_fit_entry <- paste0(doc_stem_crr_fit_hr, ' (', doc_stem_crr_fit_p, ')')
# 
# 
# # Doctorate Non-STEM
# 
# doc_dat6_non_stem <- doc_dat6 %>%
#   filter(STEM == 0)
# 
# doc_dat6_non_stem <- doc_dat6_non_stem[!is.na(doc_dat6_non_stem$event), ] %>%
#   mutate(event = ifelse(event == 1 | event == 4, 0, event)) %>%
#   mutate(event = ifelse(event == 2, 1, event)) %>%
#   mutate(event = ifelse(event == 3, 2, event)) %>%
#   mutate(TIME_DEG = ifelse(is.na(TIME_DEG), max(doc_dat6_non_stem[!is.na(doc_dat6_non_stem$TIME_DEG), ]$TIME_DEG), TIME_DEG))
# 
# doc_dat6_non_stem$Dept <- as.numeric(as.factor(doc_dat6_non_stem$Dept))
# doc_dat6_non_stem$ADMIT_TERM <- as.numeric(as.factor(doc_dat6_non_stem$ADMIT_TERM))
# 
# doc_non_stem_crr_fit <- crr(ftime = doc_dat6_non_stem$TIME_DEG, 
#     fstatus = doc_dat6_non_stem$event, 
#     cov1 = data.frame(st_required = doc_dat6_non_stem$st_required,
#                       Dept = doc_dat6_non_stem$Dept,
#                       ADMIT_TERM = doc_dat6_non_stem$ADMIT_TERM))
# 
# doc_non_stem_crr_fit_summary <- summary(doc_non_stem_crr_fit)
# doc_non_stem_crr_fit_summary <- summary_to_df_surv(doc_non_stem_crr_fit_summary)
# doc_non_stem_crr_fit_hr <- round_sig(doc_non_stem_crr_fit_summary$coef[1,2])
# doc_non_stem_crr_fit_p <- round_sig(doc_non_stem_crr_fit_summary$coef[1,5])
# doc_non_stem_crr_fit_entry <- paste0(doc_non_stem_crr_fit_hr, ' (', doc_non_stem_crr_fit_p, ')')

```


# Completed within 8/10 years

## 8

```{r message=FALSE, warning=FALSE, include=FALSE}

# Doctorate Overall
doc_completed_8 <- fit_glm_model(doc_dat6, "completed_8")
doc_completed_8_entry <- doc_completed_8$marginal_entry
doc_completed_8_summary <- doc_completed_8$marginal_summary

# Doctorate STEM
doc_stem_completed_8 <- fit_glm_model(doc_dat6, "completed_8", filter_stem = 1)
doc_stem_completed_8_entry <- doc_stem_completed_8$marginal_entry
doc_stem_completed_8_summary <- doc_stem_completed_8$marginal_summary

# Doctorate Non-STEM
doc_non_stem_completed_8 <- fit_glm_model(doc_dat6, "completed_8", filter_stem = 0)
doc_non_stem_completed_8_entry <- doc_non_stem_completed_8$marginal_entry
doc_non_stem_completed_8_summary <- doc_non_stem_completed_8$marginal_summary

```

## 10

```{r message=FALSE, warning=FALSE, include=FALSE}

# Doctorate Overall
doc_completed_10 <- fit_glm_model(doc_dat6, "completed_10")
doc_completed_10_entry <- doc_completed_10$marginal_entry
doc_completed_10_summary <- doc_completed_10$marginal_summary

# Doctorate STEM
doc_stem_completed_10 <- fit_glm_model(doc_dat6, "completed_10", filter_stem = 1)
doc_stem_completed_10_entry <- doc_stem_completed_10$marginal_entry
doc_stem_completed_10_summary <- doc_stem_completed_10$marginal_summary

# Doctorate Non-STEM
doc_non_stem_completed_10 <- fit_glm_model(doc_dat6, "completed_10", filter_stem = 0)
doc_non_stem_completed_10_entry <- doc_non_stem_completed_10$marginal_entry
doc_non_stem_completed_10_summary <- doc_non_stem_completed_10$marginal_summary

```


# Export

```{r message=FALSE, warning=FALSE, include=FALSE}

wb <- createWorkbook()

# Add sheets to the workbook
# addWorksheet(wb, "summary")
addWorksheet(wb, "stats")
addWorksheet(wb, "stats_s")
addWorksheet(wb, "stats_n")
addWorksheet(wb, "stats_1")
addWorksheet(wb, "stats_s_1")
addWorksheet(wb, "stats_n_1")
addWorksheet(wb, "stats_0")
addWorksheet(wb, "stats_s_0")
addWorksheet(wb, "stats_n_0")
addWorksheet(wb, "gpa1")
addWorksheet(wb, "gpa1_s")
addWorksheet(wb, "gpa1_n")
addWorksheet(wb, "gpa2")
addWorksheet(wb, "gpa2_s")
addWorksheet(wb, "gpa2_n")
addWorksheet(wb, "ttd")
addWorksheet(wb, "ttd_s")
addWorksheet(wb, "ttd_n")
addWorksheet(wb, "urm")
addWorksheet(wb, "urm_s")
addWorksheet(wb, "urm_n")
addWorksheet(wb, "gender")
addWorksheet(wb, "gender_s")
addWorksheet(wb, "gender_n")
addWorksheet(wb, "completed_8")
addWorksheet(wb, "completed_8_s")
addWorksheet(wb, "completed_8_n")
addWorksheet(wb, "completed_10")
addWorksheet(wb, "completed_10_s")
addWorksheet(wb, "completed_10_n")


# Write data to the sheets
# writeData(wb, sheet = "summary", x = doc_analysis_summary)
writeData(wb, sheet = "stats", x = doc_analysis$summary)
writeData(wb, sheet = "stats_s", x = doc_stem_analysis$summary)
writeData(wb, sheet = "stats_n", x = doc_non_stem_analysis$summary)
writeData(wb, sheet = "stats_1", x = doc_analysis$summary_1)
writeData(wb, sheet = "stats_s_1", x = doc_stem_analysis$summary_1)
writeData(wb, sheet = "stats_n_1", x = doc_non_stem_analysis$summary_1)
writeData(wb, sheet = "stats_0", x = doc_analysis$summary_0)
writeData(wb, sheet = "stats_s_0", x = doc_stem_analysis$summary_0)
writeData(wb, sheet = "stats_n_0", x = doc_non_stem_analysis$summary_0)
writeData(wb, sheet = "gpa1", x = doc_gpa1_model_summary)
writeData(wb, sheet = "gpa1_s", x = doc_stem_gpa1_model_summary)
writeData(wb, sheet = "gpa1_n", x = doc_non_stem_gpa1_model_summary)
writeData(wb, sheet = "gpa2", x = doc_gpa2_model_summary)
writeData(wb, sheet = "gpa2_s", x = doc_stem_gpa2_model_summary)
writeData(wb, sheet = "gpa2_n", x = doc_non_stem_gpa2_model_summary)
writeData(wb, sheet = "ttd", x = doc_time_model_summary)
writeData(wb, sheet = "ttd_s", x = doc_stem_time_model_summary)
writeData(wb, sheet = "ttd_n", x = doc_non_stem_time_model_summary)
writeData(wb, sheet = "urm", x = doc_URM_summary)
writeData(wb, sheet = "urm_s", x = doc_stem_URM_summary)
writeData(wb, sheet = "urm_n", x = doc_non_stem_URM_summary)
writeData(wb, sheet = "gender", x = doc_Non_Male_summary)
writeData(wb, sheet = "gender_s", x = doc_stem_Non_Male_summary)
writeData(wb, sheet = "gender_n", x = doc_non_stem_Non_Male_summary)
writeData(wb, sheet = "completed_8", x = doc_completed_8_summary)
writeData(wb, sheet = "completed_8_s", x = doc_stem_completed_8_summary)
writeData(wb, sheet = "completed_8_n", x = doc_non_stem_completed_8_summary)
writeData(wb, sheet = "completed_10", x = doc_completed_10_summary)
writeData(wb, sheet = "completed_10_s", x = doc_stem_completed_10_summary)
writeData(wb, sheet = "completed_10_n", x = doc_non_stem_completed_10_summary)

```



# Add separation data

```{r}

doc_sep_i_entries <- c()
doc_stem_sep_i_entries <- c()
doc_non_stem_sep_i_entries <- c()

for (year_i in 1:10) {
  
  doc_dat6_i <- doc_dat6 %>%
    filter(case_when(
      substr(ADMIT_TERM, 1, 1) %in% c("F", "X") ~ as.numeric(substr(ADMIT_TERM, nchar(ADMIT_TERM) - 3, nchar(ADMIT_TERM))) < 2023 - year_i,
      substr(ADMIT_TERM, 1, 1) %in% c("W", "S") ~ as.numeric(substr(ADMIT_TERM, nchar(ADMIT_TERM) - 3, nchar(ADMIT_TERM))) < 2024 - year_i,
      TRUE ~ FALSE
    ))
  
  # Doctorate Overall
  doc_sep_i <- fit_glm_model(doc_dat6_i, paste0("separated_", year_i))
  doc_sep_i_entry <- doc_sep_i$marginal_entry
  doc_sep_i_summary <- doc_sep_i$marginal_summary
  
  # Doctorate STEM
  doc_stem_sep_i <- fit_glm_model(doc_dat6_i, paste0("separated_", year_i), filter_stem = 1)
  doc_stem_sep_i_entry <- doc_stem_sep_i$marginal_entry
  doc_stem_sep_i_summary <- doc_stem_sep_i$marginal_summary
  
  # Doctorate Non-STEM
  doc_non_stem_sep_i <- fit_glm_model(doc_dat6_i, paste0("separated_", year_i), filter_stem = 0)
  doc_non_stem_sep_i_entry <- doc_non_stem_sep_i$marginal_entry
  doc_non_stem_sep_i_summary <- doc_non_stem_sep_i$marginal_summary
  
  # Save entries
  doc_sep_i_entries <- c(doc_sep_i_entries, doc_sep_i_entry)
  doc_stem_sep_i_entries <- c(doc_stem_sep_i_entries, doc_stem_sep_i_entry)
  doc_non_stem_sep_i_entries <- c(doc_non_stem_sep_i_entries, doc_non_stem_sep_i_entry)
  
  # Write to sheets
  addWorksheet(wb, paste0("sep", year_i))
  addWorksheet(wb, paste0("sep", year_i, '_s'))
  addWorksheet(wb, paste0("sep", year_i, '_n'))
  writeData(wb, sheet = paste0("sep", year_i), x = doc_sep_i_summary)
  writeData(wb, sheet = paste0("sep", year_i, '_s'), x = doc_stem_sep_i_summary)
  writeData(wb, sheet = paste0("sep", year_i, '_n'), x = doc_non_stem_sep_i_summary)
}

```





# Summary

```{r message=FALSE, warning=FALSE, include=FALSE}

doc_analysis_summary <- data.frame(
  Variable = c('Year 1 GPA', 'Year 2 GPA', 'URM', 'Non-Male', 'Time to Degree', 'Completed within 8 years', 'Completed within 10 years', paste0('Year ', 1:10, ' Separation')),
  All = c(doc_gpa1_entry, doc_gpa2_entry, doc_URM_entry, doc_Non_Male_entry, doc_time_entry, doc_completed_8_entry, doc_completed_10_entry, doc_sep_i_entries),
  STEM = c(doc_stem_gpa1_entry, doc_stem_gpa2_entry, doc_stem_URM_entry, doc_stem_Non_Male_entry, doc_stem_time_entry, doc_stem_completed_8_entry, doc_stem_completed_10_entry, doc_stem_sep_i_entries),
  Non_STEM = c(doc_non_stem_gpa1_entry, doc_non_stem_gpa2_entry, doc_non_stem_URM_entry, doc_non_stem_Non_Male_entry, doc_non_stem_time_entry, doc_non_stem_completed_8_entry, doc_non_stem_completed_10_entry, doc_non_stem_sep_i_entries)
)

addWorksheet(wb, "summary")
writeData(wb, sheet = "summary", x = doc_analysis_summary)

# Save the workbook with the updates
saveWorkbook(wb, paste0(dirname(getwd()), "/Shiny/Doc_Complete Summaries.xlsx"), overwrite = TRUE)

```










